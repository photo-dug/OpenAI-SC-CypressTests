{
  "name": "openai-sc-cypress-tests",
  "private": true,
  "version": "0.1.0",
  "description": "Cypress E2E for SoundCredit portal",
  "license": "MIT",
  "scripts": {
    "test": "cypress run --browser chrome --config-file cypress.config.cjs",
    "open": "cypress open --config-file cypress.config.cjs",
    "clean:reports": "rm -rf cypress/reports && mkdir -p cypress/reports",
    "zip": "node scripts/make-zip.mjs"
  },
  "devDependencies": {
    "@testing-library/cypress": "^10.0.3",
    "archiver": "^6.0.1",
    "cypress": "^14.5.4",
    "cypress-mochawesome-reporter": "^4.0.0",
    "ffmpeg-static": "^5.2.0",
    "meyda": "^5.6.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
